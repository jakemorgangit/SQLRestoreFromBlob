<Window x:Class="SQLRestoreFromBlob.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SQLRestoreFromBlob.ViewModels"
        xmlns:views="clr-namespace:SQLRestoreFromBlob.Views"
        xmlns:converters="clr-namespace:SQLRestoreFromBlob.Converters"
        Title="SQL Restore From Blob"
        Height="800" Width="1200"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBackgroundBrush}">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
        <converters:NullToVisibilityConverter x:Key="NullToVis" />

        <DataTemplate DataType="{x:Type vm:BlobConfigViewModel}">
            <views:BlobConfigView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ServerManagerViewModel}">
            <views:ServerManagerView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:BlobBrowserViewModel}">
            <views:BlobBrowserView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RestoreViewModel}">
            <views:RestoreView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="{StaticResource SidebarBackgroundBrush}">
                <DockPanel>
                    <!-- App Title -->
                    <StackPanel DockPanel.Dock="Top" Margin="20,20,20,8">
                        <TextBlock Text="SQL Restore"
                                   FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryTextBrush}"
                                   FontFamily="Segoe UI" />
                        <TextBlock Text="From Blob"
                                   FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource AccentBrush}"
                                   FontFamily="Segoe UI"
                                   Margin="0,-2,0,0" />
                        <TextBlock Text="Azure Blob Restore Utility"
                                   Style="{StaticResource SecondaryText}"
                                   Margin="0,4,0,0" />
                    </StackPanel>

                    <Border DockPanel.Dock="Top"
                            Height="1"
                            Background="{StaticResource CardBorderBrush}"
                            Margin="16,8,16,8" />

                    <!-- Connection Status -->
                    <Border DockPanel.Dock="Bottom"
                            Background="{StaticResource CardBackgroundBrush}"
                            CornerRadius="6"
                            Padding="12,8"
                            Margin="12,8,12,16">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8"
                                         Fill="{StaticResource SuccessBrush}"
                                         VerticalAlignment="Center"
                                         Visibility="{Binding IsConnectedToSql, Converter={StaticResource BoolToVis}}" />
                                <Ellipse Width="8" Height="8"
                                         Fill="{StaticResource SecondaryTextBrush}"
                                         VerticalAlignment="Center"
                                         Visibility="{Binding IsConnectedToSql, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}" />
                                <TextBlock Style="{StaticResource SecondaryText}"
                                           Margin="8,0,0,0">
                                    <TextBlock.Text>
                                        <Binding Path="ConnectedServerName" FallbackValue="Not connected" TargetNullValue="Not connected" />
                                    </TextBlock.Text>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Navigation -->
                    <StackPanel DockPanel.Dock="Top" Margin="0,8,0,0">
                        <TextBlock Text="CONFIGURATION"
                                   FontSize="10" FontWeight="Bold"
                                   Foreground="{StaticResource DisabledTextBrush}"
                                   Margin="20,8,0,4"
                                   FontFamily="Segoe UI" />

                        <RadioButton Style="{StaticResource SidebarButton}"
                                     IsChecked="True"
                                     Command="{Binding NavigateToCommand}"
                                     CommandParameter="Blob Storage">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#x2601;" FontSize="15" VerticalAlignment="Center" Width="24" />
                                <TextBlock Text="Blob Storage" VerticalAlignment="Center" />
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Style="{StaticResource SidebarButton}"
                                     Command="{Binding NavigateToCommand}"
                                     CommandParameter="SQL Servers">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#x26C1;" FontSize="15" VerticalAlignment="Center" Width="24" />
                                <TextBlock Text="SQL Servers" VerticalAlignment="Center" />
                            </StackPanel>
                        </RadioButton>

                        <Border Height="1"
                                Background="{StaticResource CardBorderBrush}"
                                Margin="16,12,16,4" />

                        <TextBlock Text="OPERATIONS"
                                   FontSize="10" FontWeight="Bold"
                                   Foreground="{StaticResource DisabledTextBrush}"
                                   Margin="20,8,0,4"
                                   FontFamily="Segoe UI" />

                        <RadioButton Style="{StaticResource SidebarButton}"
                                     Command="{Binding NavigateToCommand}"
                                     CommandParameter="Browse Backups">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#x1F4C2;" FontSize="15" VerticalAlignment="Center" Width="24" />
                                <TextBlock Text="Browse Backups" VerticalAlignment="Center" />
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Style="{StaticResource SidebarButton}"
                                     Command="{Binding NavigateToCommand}"
                                     CommandParameter="Restore">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#x267B;" FontSize="15" VerticalAlignment="Center" Width="24" />
                                <TextBlock Text="Restore" VerticalAlignment="Center" />
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Main Content Area -->
            <Border Grid.Column="1" Margin="0">
                <ContentPresenter Content="{Binding CurrentView}" Margin="24,20" />
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="1"
                Background="{StaticResource StatusBarBackgroundBrush}"
                Padding="16,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding GlobalStatus}"
                           Style="{StaticResource SecondaryText}"
                           VerticalAlignment="Center" />

                <TextBlock Grid.Column="1"
                           Text="{Binding CurrentViewName}"
                           Style="{StaticResource SecondaryText}"
                           VerticalAlignment="Center"
                           Margin="0,0,24,0" />

                <TextBlock Grid.Column="2"
                           Text="v1.0.0"
                           Style="{StaticResource SecondaryText}"
                           VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</Window>
