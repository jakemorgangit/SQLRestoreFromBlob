<UserControl x:Class="SQLRestoreFromBlob.Views.BlobBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:SQLRestoreFromBlob.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
        <converters:NullToVisibilityConverter x:Key="NullToVis" />
        <converters:FileSizeConverter x:Key="FileSize" />
        <converters:BackupTypeToColorConverter x:Key="TypeToColor" />
        <converters:DateTimeToStringConverter x:Key="DateToStr" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Browse Backups"
                       Style="{StaticResource HeaderText}" />
            <TextBlock Text="View and explore backup files stored in your Azure Blob container."
                       Style="{StaticResource SecondaryText}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <!-- Container + Database Selector -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,12">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="CONTAINER" Style="{StaticResource LabelText}" />
                        <ComboBox ItemsSource="{Binding Containers}"
                                  SelectedItem="{Binding SelectedContainer}"
                                  DisplayMemberPath="Name"
                                  Style="{StaticResource DarkComboBox}" />
                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="Load Backups"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding LoadBackupsCommand}"
                            VerticalAlignment="Bottom"
                            Margin="0,0,8,0" />

                    <Button Grid.Column="2"
                            Content="&#x21BB;"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding RefreshCommand}"
                            VerticalAlignment="Bottom"
                            ToolTip="Refresh container list" />
                </Grid>

                <!-- Server + Database Filters (shown after loading) -->
                <Grid Margin="0,12,0,0"
                      Visibility="{Binding HasFiles, Converter={StaticResource BoolToVis}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="SERVER / INSTANCE" Style="{StaticResource LabelText}" />
                        <ComboBox ItemsSource="{Binding DiscoveredServers}"
                                  SelectedItem="{Binding SelectedServer}"
                                  Style="{StaticResource DarkComboBox}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,12,0">
                        <TextBlock Text="DATABASE" Style="{StaticResource LabelText}" />
                        <ComboBox ItemsSource="{Binding DiscoveredDatabases}"
                                  SelectedItem="{Binding SelectedDatabase}"
                                  Style="{StaticResource DarkComboBox}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                        <TextBlock Text="{Binding DbSummaryText}"
                                   Style="{StaticResource SecondaryText}"
                                   Margin="0,0,0,8" />
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Summary + Filters -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Margin="0,0,0,12"
                Visibility="{Binding HasFiles, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <!-- Summary Stats (set-based, filtered) -->
                <Grid Margin="0,0,0,12"
                      Visibility="{Binding Summary, Converter={StaticResource NullToVis}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Summary.TotalSets}"
                                   FontSize="20" FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryTextBrush}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Total Sets" Style="{StaticResource SecondaryText}"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Summary.FullBackups}"
                                   FontSize="20" FontWeight="Bold"
                                   Foreground="{StaticResource AccentBrush}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Full" Style="{StaticResource SecondaryText}"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Summary.DiffBackups}"
                                   FontSize="20" FontWeight="Bold"
                                   Foreground="{StaticResource WarningBrush}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Diff" Style="{StaticResource SecondaryText}"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Summary.LogBackups}"
                                   FontSize="20" FontWeight="Bold"
                                   Foreground="{StaticResource SuccessBrush}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Log" Style="{StaticResource SecondaryText}"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Grid.Column="4" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Summary.TotalSizeDisplay}"
                                   FontSize="14" FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryTextBrush}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Size" Style="{StaticResource SecondaryText}"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>

                <!-- Filters -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DarkTextBox}"
                             Margin="0,0,12,0"
                             ToolTip="Filter by file name or database name" />

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <CheckBox Content="Full" IsChecked="{Binding ShowFull}"
                                  Style="{StaticResource DarkCheckBox}" Margin="0,0,12,0" />
                        <CheckBox Content="Diff" IsChecked="{Binding ShowDiff}"
                                  Style="{StaticResource DarkCheckBox}" Margin="0,0,12,0" />
                        <CheckBox Content="Log" IsChecked="{Binding ShowLog}"
                                  Style="{StaticResource DarkCheckBox}" Margin="0,0,12,0" />
                        <CheckBox Content="Other" IsChecked="{Binding ShowUnknown}"
                                  Style="{StaticResource DarkCheckBox}" />
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Backup Files Grid -->
        <Border Grid.Row="3" Style="{StaticResource CardBorder}">
            <Grid>
                <!-- Empty State -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding HasFiles, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}">
                    <TextBlock Text="&#x1F4C2;" FontSize="48" HorizontalAlignment="Center"
                               Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,12" />
                    <TextBlock Text="No backup files loaded"
                               Style="{StaticResource BodyText}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Select a container and click Load Backups to browse."
                               Style="{StaticResource SecondaryText}"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0" />
                </StackPanel>

                <!-- DataGrid -->
                <DataGrid ItemsSource="{Binding FilteredFiles}"
                          SelectedItem="{Binding SelectedFile}"
                          Style="{StaticResource DarkDataGrid}"
                          Visibility="{Binding HasFiles, Converter={StaticResource BoolToVis}}">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Type" Width="110" SortMemberPath="Type">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding Type, Converter={StaticResource TypeToColor}}"
                                            CornerRadius="3"
                                            Padding="8,3"
                                            HorizontalAlignment="Left"
                                            Opacity="0.85">
                                        <TextBlock Text="{Binding TypeDisplay}"
                                                   Foreground="White"
                                                   FontSize="11"
                                                   FontWeight="SemiBold" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Server / Instance" Binding="{Binding InferredServerName}" Width="150" />
                        <DataGridTextColumn Header="Database" Binding="{Binding InferredDatabaseName}" Width="150" />
                        <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="*" />
                        <DataGridTextColumn Header="Size" Binding="{Binding SizeDisplay}" Width="90" />
                        <DataGridTextColumn Header="Last Modified" Binding="{Binding LastModified, Converter={StaticResource DateToStr}}" Width="150" />
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Loading -->
                <Border Background="#80000000"
                        CornerRadius="6"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Loading backup files..."
                                   Style="{StaticResource BodyText}"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
